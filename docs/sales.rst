Sales Package
=============

The `sales` package handles all operations related to sales transactions, including managing orders, processing payments, and calculating sales metrics.

Subpackages
-----------

The `sales` package includes the following subpackages:

.. toctree::
   :maxdepth: 4

   sales.tests

Submodules
----------

The `sales` package provides the following submodules:

sales.app module
----------------

The `sales.app` module is the main entry point for sales-related operations, managing the logic for creating, updating, and deleting sales transactions.

.. automodule:: sales.app
   :members:
   :undoc-members:
   :show-inheritance:

Module Contents
---------------

The `sales` module contains all classes, functions, and constants used to manage sales transactions.

.. automodule:: sales
   :members:
   :undoc-members:
   :show-inheritance:
